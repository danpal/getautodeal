!!!
%html
  %head
    %title New cars with the best price guaranteed at localcar.co
    %meta{:content => "Get a great deal on your car easily by joining others and making dealerships compete", :name => "description"}
    %meta{:content => "group, car deal, group car", :name => "keywords"}  
    = javascript_include_tag :defaults
    = render 'layouts/stylesheets'    
    = csrf_meta_tag
  %body
    - flash.each do |key, value|
      %div{:class => "flash #{key}"}= value
    %section.container
      = render 'layouts/header'
      %section.content
        = yield
    = render 'layouts/footer'
    = render "layouts/googleanalytics"
    .debug
      = debug(params) if Rails.env.development?

